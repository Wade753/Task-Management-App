import React from "react";
import CommentManagement from "@/app/dashboard/comments/commentsManagment";

const pageComment = () => {
  return <CommentManagement initialData={[]} />;
};

export default pageComment;
